<?php

// Config file to change

$subject = "MYSERVER ".php_uname ('n'); // target server
$install_dir = "/root/lister";  // install dir where everything happens
$target_dir = '/var/www';       // target directory
$mode = 1; // 1 = new/mod 2 = new only
$run = 1; // 1 = run lister , 0 = you need to configure lister

?>